# Hardware

Canonical source: `skill_data/hardware/`. Use `tools/skill_lookup.py head <model>` for head records and `tools/head_geometry.py` for documented working-distance envelopes. Catalog repeatability is conditional and is not application accuracy. Compatibility marked `NOT_VERIFIED` is not permission to reuse a family fact.
